---
name: ashare_portfolio_allocation
description: A股统一交易员的月度全池比较、动态仓位、现金与风险控制流程
triggers:
  - 统一交易员
  - 组合仓位
  - CASH
  - 动态权重
  - 同时比较
  - 16 只股票
---

## 标准流程

1. **全池比较，自由查历史**：可调用任意 ashare-market 工具查看本地缓存中的
   全部可用历史（`lookback_days=0` 或不传即截至 `as_of` 的全部交易日），
   包括 `ashare_trader_universe_context`、`ashare_price_history`、
   `ashare_valuation`、`ashare_financials`、`ashare_ml_signal`、
   `ashare_index_history`、`ashare_cross_section_snapshot` 等；不限只数与次数。
2. **先用代码建立 20 日相对动量 top4 硬锚**：首先调用
   `ashare_momentum_baseline(as_of, window=20, top_k=4)`，使用其严格点时计算的
   全池 `rel20` 排名和前 4 名各 25%、现金 0% 的参考组合；再调用
   `ashare_market_breadth(as_of)` 获取全 A 股严格点时的
   `risk_on / neutral / defensive` 状态。不要凭正文手算或跳过。
   它是可交易的决策锚点，不是未来收益标签；不得使用未来数据，包括持有期行情、
   真实收益或事后排名。
3. **按硬门槛处理偏离**：
   - top4 总仓位必须达到任务指定的 60% 或 75%，至少保留 3 只 top4，
     非 top4 股票最多 1 只；系统会在终局确定性复核；
   - 20/60/120/250 日相对动量识别趋势持续性，5 日信号只用于判断短期加速或反转；
   - 年化波动、120/250 日最大回撤、距 250 日高点与均线偏离用于控制追高和尾部风险；
   - Qlib、估值/换手分位、最近已公告财务用于交叉验证，Qlib 不能单独否决动量股；
   - 月度路径用于识别收益是否由单个异常月份驱动。
   每个低于 25% 等权锚点或被剔除的 top4 必须同时出现至少两类独立风险：趋势破坏、波动/尾部、
   量价异常、估值过热、已公告基本面恶化、Qlib 冲突。每类最多计一次，且至少一类
   必须不是 Qlib；证据弱或相互抵消时必须保留硬锚。
4. **深挖原始序列**：需要时可对全部 16 只股票分别查看长历史原始日线、估值序列与
   财务公告，与压缩面板交叉验证；查完即进入组合决策。
5. **用市场广度定防守语境**：正文必须说明最终组合的 top4 暴露、重合项、唯一可能
   的替换项、现金偏离和市场状态。全 A 广度只用于解释现金与风险偏好，不得降低
   top4 硬锚下限，也不得替代个股双风险门槛。任何单股都不得超过 25%。
6. **检查成本与集中度**：20 日后全卖，买卖成本会侵蚀弱信号。不要为了凑满仓持有
   低置信度股票，也不要把多个高度相关的行业暴露误当成分散。
7. **使用成熟记忆**：只使用 `exit_date` 已到期的“实际组合相对动量锚点偏离损益”
   结构化归因；不要复述或继承过去的长篇理由。历史归因只用于校准偏离成本，
   不是未来收益标签，也不能替代当前面板。

## 输出硬约束

最后一行只写：

`\boxed{代码:权重,代码:权重,...,CASH:权重}`

- 权重使用 0 到 1 的小数；
- 每只股票在 `[0, 0.25]`；
- 股票与 CASH 权重之和严格为 `1`；
- 未入选股票可以省略并视为 0，但 CASH 不得省略；
- boxed 内容不得包含公司名、百分号、排名或解释文字。

## 常见陷阱

- 把 16 只股票拆成 16 个互不关联的方向预测；
- 忽略动量硬锚，或仅凭 Qlib/低估值叙事推翻多个 top4；
- 默认满仓，忽略现金也是主动仓位；
- 使用决策日之后的行情、公告、真实收益或事后排名；
- 权重看似合理但总和不等于 1、单股超过 25% 或遗漏 CASH。
