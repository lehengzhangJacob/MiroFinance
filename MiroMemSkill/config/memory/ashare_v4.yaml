# Memory & skill config for pool-v3 rerun with direction-collapse fixes
# (counter-lesson reflection, stance-balanced retrieval, neutral framing).
# Fresh namespace so results are comparable against the collapsed ashare_v3 bank.
enabled: true
store_dir: "memory_bank"
skills_dir: "memory_bank/skills_ashare"
namespace: "ashare_v4"
inject_top_k: 3
skill_top_k: 2
skill_enabled: true
reflection_enabled: true
embedding_model: "embedding-3"
inject_enabled: true
dedupe_threshold: 0.8
skill_preview_min_score: 2.0
